import AppLayout from '@/components/AppLayout'
import React from 'react'

const AddonLayout = ({ children }) => {
  return <AppLayout title="Addon">{children}</AppLayout>
}

export default AddonLayout
